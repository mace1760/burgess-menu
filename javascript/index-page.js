// This Page will deliever the content that will show on the index-page

const selectElement = document.getElementById('food-type');
const selectedValue = selectElement.value;
console.log(selectedValue);